namespace mtlpp {
    // Topics
    // Initializers
    // init(rawValue: UInt)
    // Creates a set of resource options from a raw value.
    
    enum ResourceUsage {
        Read,
        Write
    };
}